---
format: yyyy/MM/dd
---
@using RazorLight
@inherits TemplatePage<Xarial.Docify.Core.Compiler.Context.IncludeContextModel>
@{
    string GetDate()
    {
        var format = Model.Data.Get<string>("format");

        return System.DateTime.Now.ToString(format);
    }
}
@GetDate()